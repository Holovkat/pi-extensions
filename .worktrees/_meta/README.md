# Session manifests

Session manifests are generated automatically by the worktree session scripts.

Each `*.env` file here records the branch lineage and cleanup targets for one isolated session.

Do not hand-edit these files unless you are repairing a broken session workflow.
