name: pif_app
publish_to: none
environment:
  sdk: ^3.5.0
dependencies:
  flutter:
    sdk: flutter
  # Keep this fixture portable from a clean checkout: resolve the repo's pif
  # package directly instead of copying it into the sample tree.
  pif:
    path: ../../../pif
